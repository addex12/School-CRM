# 🧩 Extending the System

- Add new modules in `admin/` or `user/`
- Use MVC-inspired structure for maintainability
- Update navigation configs in `config/`
- For API integration, extend `backend/` (Node.js/Express)

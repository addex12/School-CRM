{
    "widgets": [
        {
            "title": "Total Users",
            "icon": "fa-users",
            "color": "blue",
            "count_query": "SELECT COUNT(*) FROM users"
        },
        {
            "title": "Active Surveys",
            "icon": "fa-poll",
            "color": "green",
            "count_query": "SELECT COUNT(*) FROM surveys WHERE is_active = 1"
        },
        {
            "title": "Feedback Received",
            "icon": "fa-comments",
            "color": "orange",
            "count_query": "SELECT COUNT(*) FROM feedback"
        },
        {
            "title": "Open Tickets",
            "icon": "fa-ticket-alt",
            "color": "red",
            "count_query": "SELECT COUNT(*) FROM support_tickets WHERE status = 'open'"
        }
    ],
    "sections": [
        {
            "title": "Recent Users",
            "query": "SELECT id, name, email FROM users ORDER BY created_at DESC LIMIT 5",
            "columns": ["id", "name", "email"]
        }
    ]
}
